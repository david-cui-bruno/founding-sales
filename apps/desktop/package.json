{
  "name": "@fss/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The thin Electron client: sign-in, device, session, version gate and the expiring offline cache.",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test --config playwright.config.ts"
  },
  "dependencies": {
    "@fss/contracts": "*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "esbuild": "0.21.5",
    "playwright": "^1.62.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
