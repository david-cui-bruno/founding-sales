{
  "name": "callie-sourcing-enricher",
  "version": "1.0.0",
  "private": true,
  "description": "Enrichment gate: app enrichment requests -> Tracerfy instant trace -> DNC-flagged, suppression-checked enrichment SourceEvents",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs",
    "prebuild": "npm run typecheck"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "@callie-sourcing/shared": "file:../shared",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
