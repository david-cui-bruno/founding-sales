# FSS delivery roadmap

Updated 2026-09-16 (delivery status as of the 62ffefe installation and the first real-worker call-campaign enrollment). This is the active product roadmap. Older design documents are historical context, not a required Superpowers execution process.

## The outcome

A founder should arrive at a useful queue of calls, messages worth reviewing, and meetings. Research, contact preparation, approved follow-ups, and scheduling should reduce founder homework. A large backend and a green test count do not by themselves deliver that outcome.

## Local capabilities and their limits

- Use one coherent Mac workspace across Today, Accounts, saved contacts, Import, and Settings.
- Preserve local work, selected records, and in-progress edits across navigation and temporary readiness failures.
- Create and reopen a company, run explicitly selected company research, inspect attributed source evidence, import a named person, and review the link to that saved identity.
- Open the exact saved contact, edit and save an unsent email draft, and reopen its persisted contents. Send stays held when setup or exact draft authority is unavailable.
- When a model is configured, the existing first-draft flow includes admitted company facts for one unambiguous current saved-person link. Conflicting claims, hypotheses, personal-role claims, and raw source excerpts are excluded. Source references travel in the model context, not a new persisted draft-citation record. Reopening an edited draft does not regenerate it.
- Expose guarded Phone and Worker connection settings. Pairing is not worker activation, a phone configuration is not a completed call, and company evidence is not verified contact authority.
- Edit local call capacity with revision checks. Unconfigured and zero are different values, and retained obligations are not silently discarded to satisfy a limit.
- Separate saved draft continuations from saved reply history. Unknown or older campaign templates remain read-only. Only the two recognized one-company templates, manual call and manual LinkedIn, expose reviewed approval and separate enrollment.
- With an active, configured worker and a worker-owned company, save one unapproved call campaign draft: an explicitly selected company, a meeting offer, one initial manual-call step, and lifetime caps of one call and zero email/LinkedIn steps. Saving requires current owner state and exact owner-applied projection. A pending draft keeps the same command identity through an explicit retry or queued-command reconciliation. This does not approve, enroll, activate, or start outreach.
- Review that exact saved manual-call template, explicitly approve it, then separately select a current published or confirmed business-phone route and enroll the company. Enrollment creates a due manual-call item, not a call or contact permission. Both actions recheck current owner state, preserve uncertain command identity, and require exact owner projections before showing success. Other campaign templates and multi-channel activation remain unavailable.
- On Accounts, see saved companies ranked by what is ready to prepare next, with one reason and one next-step control each: reopen an unsent draft, prepare a draft, add a business inbox route, research, or unknown when the local read fails. The ranking comes from local evidence only. The control opens the company; it never starts research, opens or sends a draft, or establishes worker ownership, and it is not an automatic daily queue.
- With the same worker and ownership conditions, choose the LinkedIn channel on the Campaigns form and save one unapproved one-company LinkedIn campaign draft, review and approve that exact template, then separately select a published or confirmed company LinkedIn route and enroll the company. Enrollment creates a due manual LinkedIn preparation item. The note stays a manual, founder-final step; nothing is sent, connected, or granted.
- From a saved scheduling reply, approve one explicit meeting slot. Approval queues exactly one owner command through the existing owner pipeline, with retry, rejection, and hold states, and keeps its command identity across an uncertain result. The Mac writes no calendar event and sends no invite. Whether the worker books anything remains unproven.

The integrated first-use regression uses the real App, preload/IPC, domain, and an encrypted disposable database. Its research pages, OS services, identities, and model responses are fictional. It validates both manual drafting and automatic generation through the real provider adapter, including company-context attribution and preservation of edited drafts. It does not establish live research/model quality, durable per-draft citations, email delivery, a process crash, or a packaged first-use journey.

The campaign-draft regression separately uses the real Campaigns route, delegation runtime, authenticated worker handler and local encrypted SQL projector, with the existing synthetic DynamoDB harness and fictional accounts. It covers owner-applied saving, separate approval with no enrollment, explicit enrollment into the real Today due queue, a fresh database connection, and same-command draft/enrollment reconciliation after route closure. It asserts zero manual phone handoffs and zero reserved/sent channel counts. It is not a deployed-worker, full preload, process-restart, phone or provider test. Renderer browser checks cover the draft form, retained input, separate review and route-selection controls, keyboard focus and theme/viewport accessibility. They use explicit saved projections, not a second simulated worker-command path. The LinkedIn launch regression follows the same shape from a clean workspace and ends with only manual preparation offered.

These local capabilities and regression results do not all describe the installed build. The installed checkpoint and the newer merged work are distinguished below. A merge alone does not update the installed app.

## Delivered checkpoint: September 16

- **Installed:** build `62ffefe` (main after PR 76), ASAR hash prefix `df0ee0fe`, installed on 17 September at 00:04 UTC with no migration, the same way as that morning's `f941035` build (ASAR prefix `7a374f3e`), each time after David ran the full `verify:release` gate himself (31 of 31 packaged end-to-end checks; the native startup dialog check needs an Accessibility-trusted terminal), took the app's own verified pre-release database copy (schema 25, 333 MB), and kept the previous `2364218` bundle aside. First launch migrated the founder database from schema 25 to 26 and retained the Lenox research facts and saved source.
- **Observed in the installed app:** one explicitly authorized Lenox company-research run at 18:40 UTC on 15 September saved four facts. Research was then paused, and the saved result remained after restart. This is evidence for that selected research and retention path, not automatic discovery, a useful generated draft, a send, or useful daily automation.
- **Observed in the installed app, 16 September (first real-company UNSENT draft journey):** on Lenox, using only the four facts and one source saved on 15 September, one explicit model preparation produced a draft in 24 seconds; the founder edited subject and body, saved locally (revision 2), navigated to Today and back, and reopened the same edited text. Nothing was sent, approved, enrolled or researched. Honest limits: the draft was grounded but generic (long subject, no value proposition, unnamed sign-off) and needed tightening before it was usable; three of four saved facts repeated one company-history paragraph; the run took about ten minutes excluding an interruption and included automation overhead, so it is not a human-speed benchmark. Friction recorded for the next slices: the inbox review required manual copying of the business address and quote from the saved source; two next-step controls did not bring their target into view; the account card stayed stale after admission until navigation; Today showed no continuation for the saved local draft; worker-scope holds cluttered the local path. Settings showed cloud work paused on this Mac and phone handoff needing confirmation, so the manual call-campaign walkthrough has not run.
- **Observed against the deployed worker, 16 September (first real-worker call-campaign walkthrough, milestone 2 up to enrollment):** the deployed worker turned out to be a 14 September build: it returned a gateway 404 for the intake configuration read and rejected the newer saved-record shape, which left one "copy company to worker" command held. David redeployed the worker at main `bafde05` from a clean worktree (Terraform plan: one route added, one Lambda code change, nothing destroyed). Reconciling the same queued command, not a new one, cleared the hold; Lenox became worker-owned; the intake read returned the honest "no configuration" answer; one manual-call campaign, version 1, was saved and separately approved on the real worker with zero reserved or sent actions. Enrollment stopped, correctly, at an empty business phone route list: the saved source shows the company's number, but the app had no step to admit a phone route from a saved source and no command to send an updated saved record to the worker after delegation. About six minutes across two resumed segments including automation overhead. Confusions recorded: campaigns listed by identifier, pre-approval wording that read like an infrastructure hold, an empty route list with no next step, and a grant-read retry line caused by a worker without Google configured. No call was placed and no phone handoff was authorized. The deployed worker is `bafde05` since 16:52 UTC; it lacks PR 71's route fix and any later worker change until the next routine redeploy, which remains a separate decision.
- **Observed on real infrastructure, 17 September (milestone 2 reached enrollment):** on the redeployed worker `62ffefe` and the installed build `62ffefe`, the founder reviewed Lenox's business phone number from the saved source (prefilled passage, one confirmation, one Admit), sent the updated saved record to the worker once ("Worker holds the current saved record"), selected the approved version-1 call campaign, chose the route `+14015723322 (published)`, enrolled once, and saw Lenox listed as due in Today ("Review company and route"). No call was placed and the phone handoff was not touched. About 7m45s across two segments including automation overhead and one stop: right after the record send the Campaigns form showed "Worker preparation is held" and "Saving is held" while the route's own local read was pending; the footer carried only non-blocking issue codes (call allocation unconfigured; two parked research jobs) and a plain re-read cleared it. The pre-release backup launcher refused the Ghostty terminal's `XDG_DATA_DIRS` once; `env -u XDG_DATA_DIRS` was the fix. Recorded for the next slices: the phone prefill chose the top-of-page number line rather than the Contact Us block; "Saved as" reads as persisted before admission; the record-send button lives under "New call campaign"; the hold line should name the local read as its cause.
- **Merged:** PRs 45 to 76 are all merged. Main is `62ffefe`, the PR 76 merge on 16 September at 21:20 UTC, and it is both the installed build and the deployed worker. PRs 60 to 62 merged that morning behind one combined gate:
  - [PR 60, ranked preparation queue](https://github.com/david-cui-bruno/founding-sales/pull/60): saved companies on Accounts ordered by local evidence, with one next-step control each. Local reads only; not an automatic daily queue and no worker ownership.
  - [PR 61, one-company LinkedIn campaign launch](https://github.com/david-cui-bruno/founding-sales/pull/61): save, approve and enroll one manual-LinkedIn template from a clean workspace on the real Campaigns route. Enrollment creates a manual preparation item; nothing is sent.
  - [PR 62, meeting approval from a saved scheduling reply](https://github.com/david-cui-bruno/founding-sales/pull/62): one explicit slot approval queues one owner command, with retry, rejection and hold states. The Mac writes no calendar event; live booking is unproven.

### Merged after the f941035 installation (all installed as 62ffefe on 17 September)

- [PR 63, route copy, CSS and roadmap tidy-ups](https://github.com/david-cui-bruno/founding-sales/pull/63), [PR 64, intake configuration write through the bridge](https://github.com/david-cui-bruno/founding-sales/pull/64) and [PR 65, packaged end-to-end specs for schema 26](https://github.com/david-cui-bruno/founding-sales/pull/65) are the three in the installed build after PR 62. Writing intake configuration is one revision-bound owner command; it grants no mailbox or calendar access.
- [PR 66, roadmap for the f941035 installation](https://github.com/david-cui-bruno/founding-sales/pull/66); [PR 67, tighter first draft](https://github.com/david-cui-bruno/founding-sales/pull/67): shorter subject, one value proposition, named sign-off, deduplicated facts, still a model draft the founder must read; [PR 68, prefilled inbox review from the saved source](https://github.com/david-cui-bruno/founding-sales/pull/68): address, passage and source preselected, tenant, emergency and maintenance addresses listed as excluded, nothing admitted without the click; [PR 69, local draft continuations on Today](https://github.com/david-cui-bruno/founding-sales/pull/69); [PR 70, honest reasons for a failed intake read](https://github.com/david-cui-bruno/founding-sales/pull/70): worker route missing, scope denied or transient, instead of one generic retry line; [PR 71, `/reply/draft` route parity](https://github.com/david-cui-bruno/founding-sales/pull/71): the handler served a route the API gateway never exposed, fixed in Terraform with a handler-versus-route parity test, effective at the next worker redeploy; [PR 72, honest Google connection status](https://github.com/david-cui-bruno/founding-sales/pull/72): a worker without Google configured, or a pairing without Google scope, no longer asks for a retry.
- Batch 4, the slices that removed the enrollment block: [PR 73, roadmap for walkthrough E part 1 and the bafde05 redeploy](https://github.com/david-cui-bruno/founding-sales/pull/73); [PR 74, Campaigns clarity](https://github.com/david-cui-bruno/founding-sales/pull/74): saved rows read company · channel · version · state, an empty route dropdown says what to do next, plain pre-approval wording; [PR 75, business phone route from the saved source](https://github.com/david-cui-bruno/founding-sales/pull/75): the phone twin of the reviewed inbox admission, prefilled and confirmed, E.164 value with a verbatim quote, nothing admitted without the click; [PR 76, send the updated saved record to the owning worker](https://github.com/david-cui-bruno/founding-sales/pull/76): one explicit owner command the worker accepts only for a company it owns, at the research revision it holds, with append-only history and evidence-backed routes, plus an automatic local comparison of the saved record with the copy last applied. The worker was redeployed at `62ffefe` on 16 September 23:22 UTC (one route added, `POST /reply/draft`; one Lambda change; nothing destroyed).

### Merged since 15 September

- Open at the previous checkpoint, merged that evening: [PR 45, company inbox draft editor](https://github.com/david-cui-bruno/founding-sales/pull/45), [PR 46, canonical reply editing backend](https://github.com/david-cui-bruno/founding-sales/pull/46), [PR 47, company phone review](https://github.com/david-cui-bruno/founding-sales/pull/47), [PR 48, ordinary saved reply editor](https://github.com/david-cui-bruno/founding-sales/pull/48). A merged editor is not a useful real-company draft, a live mailbox, or a placed call.
- [PR 49, roadmap refresh](https://github.com/david-cui-bruno/founding-sales/pull/49), [PR 50, company-only drafts from saved evidence](https://github.com/david-cui-bruno/founding-sales/pull/50), [PR 51, call enrollment kept within company route scope](https://github.com/david-cui-bruno/founding-sales/pull/51), [PR 52, reviewed inbox receipts preserved in exports](https://github.com/david-cui-bruno/founding-sales/pull/52), [PR 53, selected-company preparation into unapproved call campaigns](https://github.com/david-cui-bruno/founding-sales/pull/53), [PR 54, atomic owner mailbox scope admission](https://github.com/david-cui-bruno/founding-sales/pull/54).
- [PR 55, bounded intake configuration read](https://github.com/david-cui-bruno/founding-sales/pull/55), [PR 56, intake configuration shown during campaign preparation](https://github.com/david-cui-bruno/founding-sales/pull/56), [PR 57, enrolled LinkedIn steps connected to manual note preparation](https://github.com/david-cui-bruno/founding-sales/pull/57), [PR 58, recoverable unsent requested-email drafts from connected calls](https://github.com/david-cui-bruno/founding-sales/pull/58), [PR 59, explicit meeting approvals admitted through the owner pipeline](https://github.com/david-cui-bruno/founding-sales/pull/59). Reading configuration is not writing it; admitting an approval is not booking a meeting.
- PRs 60, 61 and 62 are described above.

The combined candidate for PRs 60 to 62 passed 9,640 root tests with one existing skip, 1,219 worker tests, types, lint, the secrets scan and 303 native-desk browser checks, and merged main `12c1f97` is identical to that gated candidate. Package, end-to-end and native acceptance were not run for these renderer, bridge and local-read slices. These results support integration of the local changes. They do not establish installed acceptance, live research/draft quality, a physical phone path, email delivery, calendar operation or useful automatic queue generation. The installed checkpoint is `62ffefe`. PRs 66 to 76 passed their combined gates (9,749; 9,774; 9,851 and 9,895 root tests; 1,219 and 1,242 worker tests; 304 to 311 native-desk browser checks) and CI before the 17 September install. A green gate and an enrollment receipt are still not a placed call, a useful automatic queue or live mailbox/calendar operation.

## What still matters next

| Priority | Outcome | Remaining work / honest limit |
| --- | --- | --- |
| 1 | Complete one useful real-company UNSENT draft journey | The company draft composer, evidence-grounded preparation and the tighter first draft are installed since 17 September; the tightening is unproven on a real company. On a separately approved candidate, use one explicitly approved property-management company and its supported route or saved contact. Review relevant research, create a useful unsent draft, edit it, navigate away and reopen it. Preserve company-only work without inventing a person. Record usefulness, elapsed time, unnecessary clicks and confusing holds. The installed research/retention checkpoint is a start, not completion of this milestone. |
| 2 | Qualify one small manual-call campaign | Real worker ownership, one saved version, one explicit approval, one reviewed phone route from the saved source, one explicit record send and one enrollment are now observed on real infrastructure, and Today lists the company as due. What remains is the separately authorized phone handoff itself, which places the call and records its outcome. Enrollment alone does not place a call. Qualify that handoff before expanding campaigns. |
| 3 | Remove founder preparation homework | Saved research feeds the existing automatic draft, and the Accounts preparation queue now ranks saved companies with one next step each from local evidence, but research/import/reviewed-link steps still require founder effort and nothing runs on its own. Use the real walkthrough to choose which preparation steps should become automatic and which genuinely need review. Measure relevance and time saved. A useful automatic daily queue remains the biggest product gap. |
| 4 | Prove ongoing live operation | With separate authorization, qualify scoped mailbox/calendar access, permitted email follow-ups, reply stops, scheduling and meeting briefs, including worker operation while the Mac sleeps. Approving a meeting slot from a saved reply now queues one owner command in source; no worker booking, calendar write or live mailbox has been qualified. Start with a tiny approved cohort. Local pairing and synthetic tests do not establish these outcomes. |
| 5 | Improve observed use before expanding | Fix friction, performance and recovery issues actually seen in the walkthrough. One-company manual LinkedIn campaigns now exist in source with founder-final, unsent notes; consider broader sequences only after the small call and LinkedIn paths prove useful in real use. Prefer measured user impact to generalized infrastructure. |

The next milestone is complete when one approved real-company journey produces a useful UNSENT draft that survives edit/reopen in the approved installed candidate, using relevant saved research. The backed-up installation and one selected research run are already recorded checkpoints, not proof that this draft milestone is complete. Any newer installation still needs separate approval, a verified profile/prior-app backup and a preservation check. Do not launch a scratch candidate against the real profile first. Record the remaining manual work. Do not count installation alone, a green test suite or an enrollment receipt as proof of an operational sales system. A useful automatic queue and live mailbox/calendar operation remain unaccepted outcomes.

## Where we overbuilt

We spent too much time repeatedly freezing files, hashing evidence, expanding negative-test matrices, and commissioning reviews for tiny changes. Some of that caught real safety defects. Repeating it for every fixture or label correction delayed the usable product, left a large unpublished branch, and made progress hard to understand.

The backend is ahead of the user journey. Historical repair-task completion is not a percentage of whole-product readiness. More tests are not a substitute for proving the next useful workflow.

## Working rules now

1. Ship small PRs organized around one user-visible outcome. The initial historical PRs are intentionally larger because they preserve the accumulated history.
2. Review actual risks, run affected tests and one combined candidate gate, and merge good changes. Preserve strict gates without adding a new ceremony for every edit.
3. Keep a short, explicit product acceptance path and fix failures directly. Do not invent new infrastructure to avoid using the product.
4. Retain honest unavailable/unknown states. Do not hide missing live setup behind fixture success or a preview screen.
5. Publishing and merging code are authorized. Installation, deployment, real-profile migration, new grants, live pairing, calls, sends, invites, purchases, and private-data exports remain separate decisions.
