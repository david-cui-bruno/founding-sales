# Closure of the 10 September adversarial audit

Date: 2026-09-17. Checked against main `abfd259`. Audit: [`docs/engineering/2026-09-10-adversarial-product-audit.md`](2026-09-10-adversarial-product-audit.md), written against source `2e629f3`. The repair program that followed it is archived at [`docs/archive/superpowers/2026-09-10-product-repair-program.md`](../archive/superpowers/2026-09-10-product-repair-program.md).

## Method

Each of the eighteen findings was re-read, then checked in two places: the current source (the files and lines quoted below, at `abfd259`) and the git history (`git log -S'<string>' -- <file>`, `git log --follow`, `git log --diff-filter=A`, `git show`). The pull request that carried each fixing commit was found with `git log --merges --ancestry-path <sha>..HEAD`. Every commit cited below resolves in this repository, and every one of them is an ancestor of the installed build `62ffefe` (checked with `git rev-list 62ffefe`), so the installed app contains these changes. The first repair commits carry the date 2026-09-09 because they were made that evening in US Eastern time, after the audit's approval at 03:04 UTC on 10 September. Nothing here is a runtime observation; it is source and history.

Labels:

- **Fixed**: the defect the audit describes is gone at `abfd259`, with the commit and the file that shows it.
- **Fixed, residual**: the reported defect is gone; a smaller item the audit also named is still open and listed under "Still open".
- **Moot after route deletion**: the surface belongs to a legacy person/prospect route that David decided on 17 September to delete in the next batch (Leads, Pipeline, Conversations, Learnings, Friday, Inbox, the lead inspector, CSV person import). The fix status is recorded anyway, because those routes are in the installed build until the deletion ships.
- **Open**: not fixed, or fixed only in part on a surface that stays.

## Summary

| # | Finding | Status | Fixing commits (PR) | Surface |
| --- | --- | --- | --- | --- |
| F01 | Editing one person's company renames other people's company | Fixed; moot after route deletion | `a9e37d6` (#4) | Leads |
| F02 | Global design changes by route | Fixed | `2b01261`, `234b819`, `3f90278` (#4) | Whole app |
| F03 | Shared overlays use the wrong theme and rail geometry | Fixed; lead inspector moot after route deletion | `2b01261`, `234b819`, `3f90278` (#4) | Lead inspector |
| F04 | Inbox offers privacy and repair actions production rejects | Fixed by removal; moot after route deletion | `33759b0` (#4) | Inbox |
| F05 | Inbox asserts health and ingestion it never checks | Fixed, residual; moot after route deletion | `33759b0`, `63932aa` (#4) | Inbox |
| F06 | Leads cannot browse beyond the first 200 people | Fixed; moot after route deletion | `cdeab7b` (#4) | Leads |
| F07 | Bulk edit's advertised selection differs from submitted targets | Fixed; moot after route deletion | `cdeab7b` (#4) | Leads |
| F08 | New-company intake stops before the useful sales workflow | Fixed, residual (open item stays) | `02bd1b3`, `abc816c`, `cef27bc`, `86f6924` (#6); `68215e1` (#45); `8ff3844` (#47); `be56adc` (#50); `26a5de3`, `9b822db` (#75) | Accounts |
| F09 | Setup instructions point to controls that do not exist | Fixed | `92de742`, `14c8dfd` (#6); `9c84145` (#8); `e2a7e62` (#18); `7e305a7` (#20) | Settings, Today |
| F10 | Campaign and approval screens lack preceding or completing actions | Fixed, residual (open item stays) | `e428e76` (#9); `fdebdb2` (#12); `c42f3e2` (#13); `14df51c` (#48); `d67cc65` (#53); `0261da6` (#57); `d49f7b0` (#58); `425237a`, `c70b515` (#61) | Campaigns, Today |
| F11 | Navigation discards company input and abandons unresolved-command state | Fixed | `c15e988` (#5); `86f6924`, `cef27bc` (#6) | Accounts, Today |
| F12 | File → Import Leads / Cmd-I is disconnected | Fixed; moot after route deletion | `14e5fa7` (#4) | Import, Leads |
| F13 | Modal and error-state interaction contracts are inconsistent | Fixed; lead inspector part moot after route deletion | `234b819`, `a5fd58e`, `cdeab7b`, `2a10167` (#4) | Palette, dialogs, lead inspector |
| F14 | Mutations lose input or hide rejection | Fixed; moot after route deletion | `cdeab7b`, `093b4f4` (#4); `f7e63b9` (#5) | Leads, Friday, Learnings |
| F15 | Diagnostics and startup failures need a reachable status model | Fixed, residual (open item stays) | `767e977` (#5) | Startup, diagnostics |
| F16 | Queue names and unavailable counts do not mean what they say | Fixed | `c15e988` (#5); `e428e76` (#9) | Today |
| F17 | Acceptance tests protect the split and substitute the page under test | Fixed, residual; residual moot after route deletion | `2b01261`, `3f90278`, `acdadca`, `2a10167` (#4); `767e977` (#5) | Tests |
| F18 | Tests and production use duplicate adapter implementations | Fixed; the adapters serve routes slated for deletion | `8830c21` (#5) | Main-process adapters |

PR numbers: #4 `3f76eee`, #5 `350a8bd`, #6 `0af7434`, #8 `7c66659`, #9 `220ce63`, #12 `6bb305e`, #13 `46ab446`, #18 `12db68b`, #20 `d88f993`, #45 `21b9757`, #47 `ab57061`, #48 `f8dfc0d`, #50 `26e4de8`, #53 `0a4fa1e`, #57 `7bde929`, #58 `b7d0134`, #61 `91124ec`, #75 `27da1c0`.

## Findings

### F01 · Editing one person's company can rename other people's company

Then: `applyLeadField` updated the shared `organizations.canonical_name`. Now: `src/main/domain/founderSalesDomain.ts:698` delegates the organization field to `assignProspectOrganization`; `:704` reads "Assign membership, never rename an identity shared with other prospects."; `:746-752` deletes only that prospect's `prospect_organizations` link and links an existing identity (normalized name or alias match) or a new one; more than one existing link or matching identity throws instead of guessing. `grep 'UPDATE organizations SET canonical_name'` finds nothing in the file. Test: `tests/integration/leadOrganizationAssignment.test.ts:117` "preserves the shared identity, aliases, property and source history when Alice moves". History: `git log -S'UPDATE organizations SET canonical_name' -- src/main/domain/founderSalesDomain.ts` → `a9e37d6` (removal, 2026-09-09) and `72280c3` (introduction, 2026-08-31). No explicit "rename this company everywhere" command was added; per-person reassignment is the only path. Leads route.

### F02 · Global design changes by route

Then: `.app-shell:has(.native-desk[data-presentation='native-a'])` selectors in `src/renderer/design/themes.css` and `src/renderer/features/today/nativeDesk.css` changed global tokens on three routes. Now: `src/renderer/app/PresentationRoot.tsx:6` renders `div.presentation-root[data-presentation='native-a']` around the whole application, including the diagnostics gate (`src/renderer/App.tsx:25`). Native tokens live on `.presentation-root[...]` in `src/renderer/design/tokens.css:77-89` and `src/renderer/design/themes.css:146-147`; `src/renderer/features/today/nativeDesk.css` has no `.app-shell` selector left. `--nav-rail-width` is defined only in `src/renderer/design/tokens.css` (216px root, 142px native, 124px below 1200px) and consumed by `src/renderer/app/shell.css:48,69`, so there is one rail-geometry source (the brief expected it in `src/renderer/app/shell.css`; it is in `src/renderer/design/tokens.css`). One wordmark: `src/renderer/app/NavigationRail.tsx:77`; `src/renderer/app/NavigationRail.test.tsx:152-158` asserts exactly one `.nav-rail__brand-native` and no "FSS" text; `tests/browser/applicationPresentation.spec.ts:217` walks all ten routes at two widths, two themes, two densities. History: `git log -S'.app-shell:has(' -- src/renderer/features/today/nativeDesk.css` → removed in `2b01261` (added by `6ae3107`, `0c502f6`, `d06d5f9`); `src/renderer/app/PresentationRoot.tsx` added in `2b01261`; `tests/browser/applicationPresentation.spec.ts` added in `3f90278`; overlay isolation in `234b819`.

### F03 · Shared overlays use the wrong theme and rail geometry

Then: the full-page contact read the root 216px because the Native override sat on a sibling of the overlay. Now: `src/renderer/features/leadInspector/leadInspector.css:436-443` keeps `left: var(--nav-rail-width)` and the variable is inherited from `PresentationRoot`, an ancestor of both shell and overlays; `src/renderer/app/overlayLayers.tsx:100` refuses to run without it. `tests/browser/applicationPresentation.spec.ts:284-308` measures the full-page left edge against the rail's right edge (at most 1px apart) and the native text colour on every route. History: root cause fixed in `2b01261`, comment and isolation in `234b819`, measurement test in `3f90278`. The full-page contact is the legacy lead inspector.

### F04 · Inbox offers privacy and repair actions that production rejects

Now: `src/renderer/features/review/ReviewDetailPanel.tsx:17` "Only unmatched Promote is currently supported by the production resolver."; `:122` "Mark personal is unavailable in this Inbox. Never Record has not been applied."; `:79` "Invariant repair is unavailable in this Inbox. Keep this evidence for a separately reviewed repair." The resolver is unchanged and still rejects everything but unmatched-communication promotion (`src/main/domain/founderSalesDomain.ts:2128-2130`, `REVIEW_RESOLUTION_UNSUPPORTED`). Neither Never Record nor arbitrary repair was implemented; the affordances were removed and the panel says so. History: `git log -S'Repair invariant' -- src/renderer/features/review/ReviewDetailPanel.tsx` → `33759b0` (removed), `03fcf25` (added). Inbox route.

### F05 · Inbox asserts health and successful ingestion it never checks

Now: the six success strings are gone; every kind's empty copy is "No items are shown in this local review snapshot. This is not an import-completeness, identity-completeness, or adapter-health check." (`src/renderer/features/review/reviewKindMeta.ts:33,38,43,48,53,58`); `src/renderer/features/review/ReviewQueue.tsx:28-37` shows neutral badges ("Not available in this Inbox", "No items in this view"). The query no longer limits before filtering: `src/main/domain/founderSalesDomain.ts:2066-2067` reads all open items, `:2094-2098` filters by kind and then pages with a cursor, `:2102` reports `totalOpenCount`, and `:2107-2110` declares the import, identity, transcript and adapter sources `not_integrated` with a null count instead of zero. The shell badge is read at workspace mount and on focus by `src/renderer/app/useReviewSummary.ts:35-37` (`src/renderer/app/FounderApp.tsx:46`), not only when Inbox mounts. History: strings removed in `33759b0`; `git log -S'ORDER BY created_at ASC, id ASC LIMIT ?' -- src/main/domain/founderSalesDomain.ts` → `63932aa` (removed); `src/renderer/app/useReviewSummary.ts` added in `63932aa`. Residual: no aggregation of the four other sources; they are shown as unavailable, which the list-reliability plan scoped deliberately (`docs/archive/superpowers/plans/2026-09-10-list-reliability-repair.md:238`). Inbox route.

### F06 · Leads cannot browse beyond the first 200 matching people

Now: the first page is still 200 (`src/renderer/features/leads/useLeadGridState.ts:11,52-53`), but `src/renderer/features/leads/LeadsRoute.tsx:54,72` keeps `nextCursor` and re-requests with it, and `src/renderer/features/leads/LeadsPage.tsx:102-105` shows "Showing N of total" with a "Load more" / "Retry more" control. `tests/browser/applicationLeadsReliability.spec.ts:191,206` imports 208 people, arrow-keys across the 200/201 boundary and opens row 208. History: `git log -S'Load more' -- src/renderer/features/leads/LeadsPage.tsx` → `cdeab7b`; the spec was added in the same commit. Leads route.

### F07 · Bulk edit's advertised selection differs from submitted targets

Now: `src/renderer/features/leads/useLeadMutations.ts:99-100` submits every checked id regardless of the current filter; `:66` refuses more than 200 without submitting; `:75-76` verifies the receipt's affected ids equal the submitted set ("Receipt scope mismatch") before clearing anything; `src/renderer/features/leads/LeadsBulkBar.tsx:34` reads "N selected · M outside view". The old intersect-then-clear code in `src/renderer/features/leads/LeadsRoute.tsx` is gone; the route delegates to the hook (`src/renderer/features/leads/LeadsRoute.tsx:110`). History: `src/renderer/features/leads/useLeadMutations.ts` created in `cdeab7b`. Leads route.

### F08 · New-company intake stops before the useful sales workflow

Then: the local workspace contract had six operations and the account view was read-only. Now: `src/shared/contracts/localWorkspaceContract.ts:132-135,143-144` adds `prepareCompanyDraft`, `admitCompanyDraftEmail`, `admitCompanyPhoneRoute`, `linkCompanyPerson` and `researchCompany`, and `:91` a per-company next-step enum (`reopen_draft`, `draft`, `add_route`, `research`, `unknown`). `src/renderer/features/today/LocalAccountLibrary.tsx:82` composes the research panel, contact link, draft editor and phone-route review for the selected company; `src/renderer/features/today/CompanyPhoneCall.tsx:246` no longer tells the user to link a contact ("No person is required for an eligible company phone route"). History: `researchCompany` in `02bd1b3`, `linkCompanyPerson` in `abc816c`, research panel in `86f6924`, contact link in `cef27bc` (all PR #6); draft editor `68215e1` (#45); the old "link a real contact route" copy removed in `8ff3844` (#47); `prepareCompanyDraft` in `be56adc` (#50); phone route admission `26a5de3` and `9b822db` (#75). Residual, open: there is still no operation or control to edit a saved company's name or domain (`grep -in rename src/shared/contracts/localWorkspaceContract.ts` finds nothing), and the account row still carries the subtitle "Read-only local evidence" (`src/renderer/features/today/LocalAccountLibrary.tsx`). Accounts stays, so this item stays open.

### F09 · Setup instructions point to controls that do not exist

Now: Settings has Phone, Call capacity and Worker connection sections (`src/renderer/foundation/SettingsScreen.tsx:65-67`, mounted at `:491-500`). `src/renderer/foundation/PhoneSetupSection.tsx:43,88,100` is a production consumer of the phone setup contract (status, confirm, clear over `phone-setup:*` channels, `src/preload/apis/phoneSetupApi.ts:14`); `src/renderer/foundation/WorkerSetupSection.tsx:81,146` reads delegation status and redeems a pairing (`src/main/ipc/registerOutreachIpc.ts:111`); `src/renderer/foundation/CallCapacitySection.tsx:79` writes call capacity through `local-workspace:update-call-settings`, whose domain handler `src/main/domain/founderSalesDomain.ts:1249-1252` is now the production caller of `updateMeetingFirstAccountCallSettingsCas`. Native Desk's "Review Settings" links open those sections (`src/renderer/features/today/NativeDeskRoute.tsx:763,808`). History: `src/renderer/foundation/PhoneSetupSection.tsx` added in `92de742`, `src/renderer/foundation/WorkerSetupSection.tsx` in `14c8dfd` (#6), `src/renderer/foundation/CallCapacitySection.tsx` in `9c84145` (#8), `src/renderer/foundation/WorkspaceAccessSection.tsx` in `e2a7e62` (#18), `src/renderer/foundation/RemoteGoogleConnectionsSection.tsx` in `7e305a7` (#20). `src/renderer/foundation/ConnectionsSection.tsx` is unchanged in role (desktop OpenAI and Gmail); it is no longer the only setup surface.

### F10 · Campaign and approval screens lack preceding or completing actions

Now: the one-company template path is real. Campaigns mounts `CallCampaignDraft` (`src/renderer/features/today/NativeDeskRoute.tsx:788`) and `CampaignReview`, `ManualLinkedInPreparation` and `CallCampaignEnrollment` (`:936-943`); `src/renderer/features/campaigns/CallCampaignDraft.tsx:206` submits the draft command, `src/renderer/features/campaigns/CallCampaignEnrollment.tsx:203-212` submits approval and then enrollment, for the call and LinkedIn templates. Non-template versions are labelled honestly: `src/renderer/features/campaigns/CampaignReview.tsx:31` "Read-only preview. Editing, approval, enrollment and activation are not available here." LinkedIn first-use preparation has a caller (`src/renderer/features/linkedin/ManualLinkedInPreparation.tsx:155,183`) and so does requested-email drafting (`src/renderer/features/today/RequestedEmailPreparation.tsx:82`, mounted from `src/renderer/features/today/CompanyPhoneCall.tsx:282`). The "Needs your approval" grouping is gone (`grep -rn 'Needs your approval' src/renderer` finds nothing; removed in `e428e76`). History: `e428e76` (#9), `fdebdb2` (#12), `c42f3e2` (#13), `14df51c` (#48), `d67cc65` (#53), `0261da6` (#57), `d49f7b0` (#58), `425237a` and `c70b515` (#61). Residual, open: ordinary reply approval is still an explicit hold, "Reply approval held: an exact permission binding is unavailable here. Saving is not approval or sending." (`src/renderer/features/today/OrdinaryReplyEditor.tsx:9`); it waits for the Gmail follow-up plan (roadmap priority 4). The meeting-approval sub-flow added by PR #62 (`2f4ad0e`, `f665a3b`; `src/renderer/features/meetings/MeetingApproval.tsx`, imported at `src/renderer/features/today/OrdinaryReplyEditor.tsx:6` and `src/renderer/features/today/DailyAnswers.tsx:4` at `abfd259`) is removed by Batch 5 lane A per the 17 September decision.

### F11 · Navigation discards company input and can abandon unresolved-command UI state

Now: `src/renderer/app/FounderApp.tsx:34` places `LocalCompanyIntakeProvider` above the keyed route element (`:70`, still keyed by design), and `src/renderer/App.tsx:28` places it above the admitted/diagnostics branch, so the intake form, the exact pending request and the selected company survive navigation and readiness changes; routes borrow the owner through `useFirstUseContinuation` (`src/renderer/features/today/LocalCompanyIntake.tsx:57-61`, `src/renderer/features/today/localCompanyContinuation.ts:27`). Tests: `src/renderer/app/FounderApp.test.tsx:324` "keeps the local company draft across real Accounts route navigation", `:957` Campaigns and back retain the selected account and the same pending request without automatic execution. History: `c15e988` (#5), `86f6924` and `cef27bc` (#6).

### F12 · File → Import Leads / Cmd-I is disconnected

Now: `src/renderer/app/FounderApp.tsx:59` listens for `callie:open-import` and opens the global import dialog; the producer `src/main/applicationMenu.ts:40-42` is unchanged; producer and mounted consumer are tested together in `src/renderer/app/FounderApp.test.tsx:399` and `tests/browser/applicationPresentation.spec.ts:400,457`. The baseline had no listener (`git show 2e629f3:src/renderer/app/FounderApp.tsx` contains no `open-import`). History: `14e5fa7` (#4). Residual: the comment at `src/main/applicationMenu.ts:36-38` still says the event is "Harmless when the renderer has no listener yet"; it is stale but harmless. Import and Leads.

### F13 · Modal and error-state interaction contracts are inconsistent

Now: `src/renderer/app/overlayLayers.tsx:56,109-115` keeps one registry of open layers and one document Escape handler that dismisses only the topmost layer (modal first); `src/renderer/app/useModalDialog.ts:41,48-57,76-89` uses native `showModal()`, contains Tab and recovers focus; `src/renderer/app/commandPalette/CommandPalette.tsx:53,127-130` uses it. `src/renderer/features/leadInspector/LeadFullPage.tsx:60` and `src/renderer/features/leadInspector/LeadInspector.tsx:196` register as nonmodal layers instead of an unconditional document listener, and both render the header with its Close control before the loading and error branches (`src/renderer/features/leadInspector/LeadFullPage.tsx:69`, `src/renderer/features/leadInspector/LeadInspector.tsx:223-227`, `src/renderer/features/leadInspector/InspectorHeader.tsx:31,48-49`). `tests/browser/applicationModals.spec.ts:151` exercises seven real modal consumers and `:405` checks the palette's Escape does not clear the Leads bulk selection. History: `234b819`, `a5fd58e`, `cdeab7b`, `2a10167` (#4). No browser case reproduces the exact palette-over-full-page stacked Escape; the mechanism is in source and the lead inspector is a legacy surface.

### F14 · Several mutations lose input or hide rejection instead of reporting an outcome

Now: Leads' refresh reports success, failure or stale (`src/renderer/features/leads/LeadsRoute.tsx:28,36,40`); `src/renderer/features/leads/useLeadMutations.ts:6,57,61-62` keeps the editor open with a failed status on rejection, refuses a second pending change, and `:81-83` distinguishes "Saved" from "Saved; list refresh failed" so a read failure never invites a duplicate write. Friday admits one intent synchronously, holds it as unconfirmed with the input kept, and offers a frozen-intent retry (`src/renderer/features/friday/FridayRoute.tsx:30,131,137-140,189-192`). Capture learning guards repeated Save with a pending ref and blocks dismissal while pending (`src/renderer/features/learnings/CaptureLearningDialog.tsx:45,49,58,167`). History: `f7e63b9` (#5), `cdeab7b` and `093b4f4` (#4). Leads, Friday, Learnings.

### F15 · Diagnostics and startup failures need a current, reachable status model

Now: `src/renderer/foundation/useFoundationHealth.ts:59,69,77,79-83` times a read out at 15 s, re-reads every 60 s while visible and on focus or visibility change, and records `checkedAt`; `src/renderer/foundation/DiagnosticsScreen.tsx:64,73` shows the last successful read, marks it stale after a failed refresh, and states that Refresh reads but does not repair; `src/renderer/App.tsx:40` shows the observation status in the admitted shell. A rejected startup reaches a native dialog with Quit and Restart (`src/main.ts:158-168,327,332-334`, `src/main/startupFailureDialog.ts:4-8`, "Callie could not start") instead of a silent quit. History: `767e977` (#5). Residual, open: `src/main/health/healthService.ts:54` still derives the Operations summary from sourcing health alone (last touched in `16af00b`, 4 September). That summary describes the legacy sourcing stack; it goes with the stack or gets a real source.

### F16 · Queue names and unavailable counts do not consistently mean what they say

Now: Today separates a "Local commitments" lane (six labelled kinds, `src/renderer/features/today/RetainedWork.tsx:8`) from "Calls", which lists only the call snapshot (`src/renderer/features/today/NativeDeskRoute.tsx:797-805`). Every count goes through `VisibleCount` (`src/renderer/features/today/visibleCount.ts:4-9,23`): when the worker scope is unavailable (`src/renderer/features/today/NativeDeskRoute.tsx:630,720`) Accounts and Campaigns render "Unavailable", and after a failed read "last known" or "partial", never a measured zero; empty states name the unavailable scope (`:853,869,875`). `tests/browser/startupPresentation.spec.ts:219` asserts the lane names. History: `c15e988` (#5), `e428e76` (#9), later `22f5b9a`.

### F17 · Acceptance tests protect the split and sometimes substitute the page under test

Now: `tests/browser/startupPresentation.spec.ts:224-233` keeps the legacy sample inside the A-presentation check instead of restoring the old brand; `src/renderer/features/today/NativeDeskPresentation.test.tsx:92` requires the common presentation on all three desk surfaces; `tests/e2e/bauhausWorkflow.spec.ts:50` asserts the native canvas colour (the cream value is gone); `tests/fixtures/nativeDeskBrowser.tsx:20` throws for any non-desk route instead of substituting Today, and `tests/browser/nativeDesk.spec.ts:1101-1102` asserts that; the real application is exercised through `tests/fixtures/applicationPresentationBrowser.tsx:9` with a route oracle that imports the production `appRoutes` and holds a per-route heading and read proof for all ten routes (`tests/support/presentationOracle.ts:2,7-18,150,177`, `tests/browser/applicationPresentation.spec.ts:225-250`); `src/renderer/app/FounderApp.test.tsx:157` supplies the daily API. History: `3f90278`, `2b01261`, `acdadca` (`git log -S'246, 240, 223' -- tests/e2e/bauhausWorkflow.spec.ts`), `2a10167` (#4); `767e977` (#5). Residual: `tests/e2e/bauhausWorkflow.spec.ts:6` still iterates a hard-coded eight-route list without Accounts and Campaigns; that packaged spec covers the legacy routes and goes with them.

### F18 · Tests and production use duplicate adapter implementations

Now: the nine direct-domain factory bodies are deleted. `git show --stat 8830c21` removes them from `src/main/conversations/conversationsService.ts`, `src/main/friday/fridayService.ts`, `src/main/imports/importService.ts`, `src/main/leads/leadDetailService.ts`, `src/main/leads/leadsService.ts`, `src/main/learnings/learningsService.ts`, `src/main/pipeline/pipelineService.ts`, `src/main/review/reviewService.ts` and `src/main/today/todayService.ts`, and adds `tests/integration/productionProviderReadiness.test.ts`, `tests/fixtures/productionDomainGate.ts` and cases in `tests/main/registerApplicationIpc.test.ts`. The shipped mappings in `src/main/ipc/registerApplicationIpc.ts:84-224` are the only adapters and are exercised through a real encrypted `FoundationRuntime` (`tests/integration/productionProviderReadiness.test.ts:125`); `src/main/today/todayService.ts` is 49 lines of types; `src/main/leads/leadDetailService.ts:45` keeps `unavailableOutboundCapabilities`, as the audit required. History: `8830c21` (#5). The 9 September engineering notes do not record this retirement; the spec table at `docs/archive/superpowers/specs/2026-09-10-workspace-continuity-repair.md:169-181` and the commit do.

## Still open after this closure

1. **F08**: no operation or control edits a saved company's name or domain; the account row subtitle still reads "Read-only local evidence" (`src/renderer/features/today/LocalAccountLibrary.tsx`). Accounts stays; this is the one product item from the audit that remains open on a kept surface.
2. **F10**: ordinary reply approval is an explicit hold (`src/renderer/features/today/OrdinaryReplyEditor.tsx:9`). It is not a defect but an unbuilt path; it belongs to roadmap priority 4 (Gmail follow-ups from the Workspace mailbox).
3. **F15**: `src/main/health/healthService.ts:54` derives the Operations summary from sourcing health alone. Resolve together with the sourcing-stack decision.
4. **F02, copy**: `src/renderer/foundation/ConnectionsSection.tsx:93` still says "record opt-outs in FSS"; plain text, not a wordmark, on a Settings section that describes the desktop Gmail connection.
5. **F12, comment**: `src/main/applicationMenu.ts:36-38` still calls a missing listener harmless; the listener exists.
6. **Moot residuals** (go with the legacy routes): F05's four `not_integrated` Inbox sources; F13's missing stacked-Escape browser case over the lead full page; F17's eight-route list in `tests/e2e/bauhausWorkflow.spec.ts:6`.

## Effect of the 17 September decisions

F01, F04, F05, F06, F07, F12 and F14 live entirely on routes that the next batch deletes; F03 and F13 mostly concern the lead inspector; F17's remaining item is the packaged legacy-route spec; F18's adapters serve those same routes. Their fixes were worth having (they are in every build installed since 11 September, including `62ffefe`) and they will leave with the routes, together with their tests (`tests/browser/applicationLeadsReliability.spec.ts`, `tests/fixtures/applicationLeadsScenario.ts`, `tests/e2e/bauhausWorkflow.spec.ts`, the legacy cases of `tests/integration/productionProviderReadiness.test.ts`), while tables, migrations and suppression data stay. What the audit called the four integration failures is closed in source: presentation is owned above every route (F02, F03, F17), capabilities are joined to first-use entry points on the company model (F08, F09, F10, F11), compatibility assertions no longer stand in for product acceptance (F17), and the real registry is exercised end to end (F17, F18). What remains is product work, not repair: an automatic morning list and a call from Today.

## How to re-verify

```bash
export PATH="/opt/homebrew/opt/node@24/bin:/opt/homebrew/bin:$PATH"
git log --no-walk --format='%h %ad %s' --date=short a9e37d6 2b01261 33759b0 63932aa cdeab7b c15e988 767e977 8830c21   # commits resolve
git log --oneline --merges --ancestry-path a9e37d6..HEAD | grep -i 'pull request' | tail -1                            # the PR that carried a commit
grep -rn -- '--nav-rail-width:' src/renderer                                                                            # one definition site (tokens.css)
grep -rn 'Needs your approval' src/renderer; grep -c 'UPDATE organizations SET canonical_name' src/main/domain/founderSalesDomain.ts
npx vitest run tests/integration/leadOrganizationAssignment.test.ts tests/integration/productionProviderReadiness.test.ts src/renderer/app/NavigationRail.test.tsx
```
