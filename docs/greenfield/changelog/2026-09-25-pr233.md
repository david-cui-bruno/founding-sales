- 2026-09-25 PR 233 (g91): an app-only merge to main deploys itself to production — promoted by digest, the worker then the API rolled and held to their digests, smoked with no credential — while any commit since production's that touches infrastructure, a migration, a release script or a workflow answers "manual", and the first app merge after it is released by hand
