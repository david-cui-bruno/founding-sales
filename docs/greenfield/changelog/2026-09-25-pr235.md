- 2026-09-25 PR 235 (g96): the release record the worker sends under now comes from the green Greenfield gate run on the deployed commit, not from a full rehearsal: `infra/scripts/release-record-from-ci.sh <gate run> <commit> <api digest> <worker digest> [--enables-sending]` checks the gate and images runs on GitHub and writes a `source: "ci-gate"` record for `release-deploy.sh --release-record`; sending is still the deployment flag plus the owner's attestation
