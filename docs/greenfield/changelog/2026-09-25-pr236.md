- 2026-09-25 PR 236 (g95): the previous-generation app is gone from the repository (`src/`, `client/`, `cloud/`, `native/`, `tests/`, its scripts, configs, docs, CI jobs and root dependencies; the tag `legacy-final` holds it), so `npm ci` installs 482 packages instead of 1,024 and CI runs only the greenfield workflows plus the secret scan and the root-scripts lint
