- 2026-09-25 PR 239 (g99): no alarm e-mails the owner when it trips; one e-mail a day, `Callie daily alarm digest — <date>` at 07:00 New York time, lists every alarm not OK and the last 24 hours of state changes, or `All N alarms OK.` when nothing happened (the owner's decision 11C; supersedes the per-composite e-mails of g62 and g81). Read one immediately with `aws cloudwatch describe-alarms --state-value ALARM`. Both deployment-role policies must be re-put before the next infrastructure apply, which adds the digest function and its schedule
