- 2026-09-25 PR 242 (g100): the CI deploy now puts the release record for the digests it deployed, after the smoke passed (`ci-deploy-app.sh record` on the operations task, from the green gate run on the images commit), and the owner's sending attestation may name the release process, `"releaseGateReference": "ci-gate:main"` (any ci-gate record naming the running image), so sending stays on across automatic deploys; a record that is not put fails the run red with one line and the new worker holds until it is put by hand (release.md 4.0). The operator sets four repository variables from the production root's new `ci_deploy_*` outputs, and `fss-prod-ci-deploy` gains `ecs:RunTask` on the operations family in the production cluster and `ecs:TagResource` only as part of it.
