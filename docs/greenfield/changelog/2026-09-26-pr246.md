- 2026-09-26 PR 246 (A1): an automatic deploy now starts only when the Greenfield gate and the Source security gate (each found by its workflow file) are green on the images commit and that commit is still on main. It waits up to thirty minutes with no credential, otherwise says `manual` with the reason, and checks again before its first write and before the record put. It deploys only digests from the images run's own artifact whose images that run pushed. A change to `schemaRange.ts`, `migrationRunner.ts` or `queryable.ts` takes the manual path. A rollout counts only once ECS calls it COMPLETED and every running task has the new digest, which `deployed-digests.sh` now requires too. No IAM change
