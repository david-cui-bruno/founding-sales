#!/usr/bin/env bash
# Old name of infra/scripts/lib.sh (P7, 26 September 2026), kept for the callers not yet
# moved; deleted once they have.
# shellcheck source=infra/scripts/lib.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/lib.sh"
